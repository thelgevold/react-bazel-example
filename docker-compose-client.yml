version: "3.5"

networks:
  caching_remote-cache:
    external: true

services:
  api:
    build: .
    command: bash -c "npm run start-backend"
    ports:
      - "7001:7001"
    container_name: api
    volumes:
      - ./src:/usr/src
    networks:
      caching_remote-cache:
        aliases:
          - api